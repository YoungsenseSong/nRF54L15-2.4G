RF Link Development Log
=======================

2026-04-16 - Dual-core IPC base and RF application split
What changed:
- Added applications/rf_link_tx as a dual-core sysbuild application.
- Added applications/rf_link_tx/flpr_app as the cpuflpr LP child image.
- Added applications/rf_link_rx as the single-core receiver application.
- Added common frame structure in rf_link_tx/src/proto.h and rf_link_rx/src/proto.h.
Why:
- Keep the final acquisition-side structure close to the planned HP/LP split.
- Keep RX simple so link issues are easier to isolate.
Observed:
- TX/RX build with NCS v3.1.0 and nRF54L15 Connect Kit board targets.
Result:
- Basic compile-level application structure is present.
Next:
- Bring up IPC, ESB, and serial statistics.

2026-04-16 - LP fake sampling and HP IPC receive path
What changed:
- flpr_app/src/adc_sampler.c generates deterministic 16-bit sample frames.
- flpr_app/src/ipc_tx.c sends complete rf_frame payloads through ipc_service.
- rf_link_tx/src/ipc_bridge.c receives and validates IPC frames on HP.
- rf_link_tx/src/tx_queue.c buffers HP-side frames before RF send.
Why:
- Use fake data first so ADC driver work is not mixed with RF bring-up.
- Keep a queue boundary between IPC receive and RF send.
Observed:
- HP receives LP frames and can enqueue them.
Result:
- LP -> IPC -> HP pipeline established.
Next:
- Add ESB PTX/PRX link and RX-side statistics.

2026-04-16 - First ESB private 2.4 GHz link
What changed:
- rf_link_tx/src/radio_link.c configures ESB PTX on channel 40.
- rf_link_rx/src/radio_link.c configures ESB PRX with matching parameters.
- rf_link_rx/src/rx_reorder.c checks magic, size, sequence, lost frames, and samples.
- Debug output uses uart_poll_out() instead of printk.
Why:
- ESB gives ACK/retransmission and is faster to validate than a bare RADIO state machine.
- Direct UART was already proven more reliable for this dual-core setup.
Observed:
- RX prints frames, samples, bps, lost, dup, bad, seq, first, and last.
Result:
- Private 2.4 GHz path is functional.
Next:
- Record CSV stats and add latency measurements.

2026-04-19 - MAC latency and CSV capture
What changed:
- rf_link_tx/src/radio_link.c measures MAC latency from esb_write_payload() to completion.
- rf_link_tx/src/main.c prints mac_cnt, mac_last_us, mac_min_us, mac_avg_us, mac_max_us.
- save_serial_csv.py parses TX stat and RX stat lines into CSV.
- RX UART output was kept to application statistics only, not raw sample dumps.
Why:
- Need latency data without polluting the UART with payload samples.
- CSV output is needed for later paper/patent experiment records.
Observed:
- TX prints MAC latency statistics.
- RX output remains compact and machine-parseable.
Result:
- PC-side CSV capture can record both TX and RX status streams.
Next:
- Run sustained 50 kbps and higher-throughput tests.

2026-04-19 - 50 ksps x 16-bit stress setting
What changed:
- rf_link_tx/src/proto.h changed TX period from 10 ms to 640 us.
- rf_link_tx/flpr_app/src/main.c now sleeps with K_USEC(RF_LINK_TX_PERIOD_US).
Why:
- 32 samples per frame at 640 us/frame equals 50 ksps.
Observed:
- RX effective payload is about 286 to 306 kbps, not 800 kbps.
- RX lost counter rises quickly.
- TX q_drop rises quickly while rf_fail stays low.
- TX MAC latency min/avg is about 1087/1574 us, longer than 640 us.
Result:
- Current 1 Mbps ESB + ACK configuration is throughput-limited for 50 ksps.
Next:
- Evaluate 2 Mbps/4 Mbps PHY, reduced ACK overhead, larger frames, or streaming no-ACK mode.

2026-04-23 - v0.5 RF throughput optimization build
What changed:
- rf_link_tx/src/proto.h and rf_link_rx/src/proto.h increased frame payload from 32 to 96 samples.
- rf_link_tx/src/proto.h and rf_link_rx/src/proto.h set the 50 ksps period to 1920 us and status period to 1000 ms.
- rf_link_tx/src/radio_link.c and rf_link_rx/src/radio_link.c select ESB 4 Mbps on nRF54L15, with 2 Mbps fallback.
- rf_link_tx/src/radio_link.c sends streaming payloads with no-ACK and disables retransmission count.
- rf_link_rx/src/radio_link.c enables matching selective auto-ACK configuration for no-ACK payload handling.
- rf_link_tx/src/tx_queue.c increases HP queue depth from 16 to 64 frames.
- rf_link_tx/flpr_app/src/main.c removes the LP software queue from the active path and sends generated frames directly over IPC.
- rf_link_tx/flpr_app/src/ipc_tx.c reduces IPC retry sleep from 1 ms to 50 us.
- rf_link_tx/flpr_app/CMakeLists.txt stops building the inactive sample_buffer.c helper.
- rf_link_tx/src/main.c changes TX status printing from packet-count-based to 1000 ms time-based output and shortens RF send timeout to 5 ms.
- rf_link_rx/src/main.c changes RX status output to the shared 1000 ms status period.
- rf_link_tx/prj.conf and rf_link_rx/prj.conf raise ESB max payload and FIFO sizes for 204-byte frames.
Why:
- v0.4 showed the 1 Mbps ACK path had MAC latency longer than the 640 us production period.
- Larger frames reduce packet rate from 1562.5 pps to about 520.8 pps for the same 50 ksps payload.
- No-ACK streaming removes ACK turnaround from the payload data path.
Observed:
- TX dual-core sysbuild completes successfully.
- RX single-core build completes successfully.
Result:
- v0.5 firmware is ready for hardware UART/CSV throughput measurement.
Next:
- Flash both boards, capture TX/RX CSV for at least 60 seconds, and update EXP-001 with actual bps, lost, q_drop, and latency.

2026-04-24 - Zero-throughput debug closure for the 204-byte IPC/RF path
What changed:
- rf_link_tx/flpr_app/src/ipc_tx.c now accepts both return 0 and returned byte count as successful ipc_service_send() results, and retries -ENOMEM, -EAGAIN, -EBUSY, and -ENOBUFS.
- rf_link_tx/src/lp_trace.h plus both TX overlay files add a shared LP trace area with boot, stage, sequence, IPC, and fatal counters.
- rf_link_tx/src/main.c prints ipc_bad_size, ipc_bad_magic, lp_stage, lp_boots, lp_fatal, lp_fatal_reason, lp_loop, lp_seq, lp_ok, lp_busy, lp_fail, and lp_ret.
- rf_link_rx/src/main.c prints rf_evt, rf_frames, and rf_read_err so "no packets on air" and "packets read but rejected" are separated.
- rf_link_tx/src/fatal_diag.c and rf_link_tx/flpr_app/src/fatal_diag.c add HP fatal UART output and LP fatal trace capture; both TX CMakeLists now build these sources.
- rf_link_tx/prj.conf and rf_link_tx/flpr_app/prj.conf set CONFIG_PBUF_RX_READ_BUF_SIZE=256, and rf_link_tx/src/proto.h asserts that the PBUF RX buffer is not smaller than the 204-byte wire frame.
Why:
- After the v0.5 parameter change, TX first showed LP IPC bound but no frames were forwarded to RF, and RX stayed at zero.
- After the first IPC-side fix, TX exposed HP fatal reason=4 during ipc_bridge_init(), which pointed to an ICMSG-side assertion instead of an RF problem.
Observed:
- On Zephyr ICMSG, ipc_service_send() success can return the sent byte count instead of 0.
- Once real 204-byte frames started flowing, the default 128-byte PBUF RX buffer caused an ICMSG-side assert and TX restart.
Result:
- TX now reaches lp_stage=4 with sent=ipc_rx=queued=rf_ok and q_drop=0 in stable excerpts.
- RX now reports frames, samples, and payload bitrate instead of staying at zero.
Next:
- Remove the remaining throughput gap between the requested 1920 us period and the measured payload rate.

2026-04-24 - Absolute deadline pacing closes the remaining 50 ksps gap
What changed:
- rf_link_tx/flpr_app/src/main.c now supports absolute-deadline pacing based on k_cycle_get_64().
- rf_link_tx/src/proto.h adds the RF_LINK_LP_USE_ABSOLUTE_PACING switch so deadline pacing can be disabled without reverting code.
- rf_link_tx/src/main.c prints lp_pace=deadline or lp_pace=relative at boot.
Why:
- Relative k_sleep(K_USEC(1920)) made the real frame period equal to processing time plus 1920 us, which limited stable payload to about 504 fps or about 774 kbps.
Observed:
- Before deadline pacing, TX and RX were healthy but payload rate still lagged the 800 kbps target by about 3 percent.
- After deadline pacing, operator bench runs reported the firmware stable near the 50 ksps x 16-bit target.
Result:
- The current working state is close to a full 16-bit x 50 ksps transport path.
Next:
- Capture longer-duration CSV evidence and replace the fake sampler with ADC DMA when the acquisition side is ready.

2026-04-24 - v0.6 RX binary sample stream export
What changed:
- applications/rf_link_rx/src/rx_sample_stream.c and rx_sample_stream.h add an optional accepted-frame binary export path on uart30.
- applications/rf_link_rx/stream.conf enables the export mode, sets uart30 to 2000000 baud at runtime, and enables UART runtime configure support.
- applications/rf_link_rx/src/rx_reorder.c now taps each accepted rf_frame into the optional stream queue.
- applications/rf_link_rx/src/main.c keeps the default statistics-only build unchanged and suppresses text RX status output when stream mode is active.
- dump_rx_frames.py captures the binary records on the PC side and saves one CSV row per accepted RF frame under 2.4g_results.
- applications/rf_link_rx/sample.yaml adds a build-only stream variant so CI can compile the export mode explicitly.
Why:
- save_serial_csv.py only stores UART statistics, not real received samples.
- Future ADC integration and downstream processing need a host-visible export path for accepted RF frames.
- The default stable RF link should remain untouched until stream mode is explicitly requested.
Observed:
- The default build_rf_link_rx build still succeeds and produces merged.hex.
- A dedicated stream.conf build also succeeds locally.
- dump_rx_frames.py passes record-format self-test with the firmware-side 224-byte binary record.
Result:
- v0.6 adds a real RX sample export path without changing the default link structure or default statistics workflow.
Next:
- Flash the stream build to RX, capture COM7 at 2000000 baud, and verify on hardware that stream_drop_total stays low while RF throughput remains stable.

Detailed Version Notes
======================

Version focus: v0.5 throughput optimization and bring-up closure
---------------------------------------------------------------

Goal
- Carry `16-bit * 50 ksps = 800000 bps` of useful sample payload.
- Keep the TX architecture as `LP generate -> IPC -> HP queue -> ESB PTX`.

Why v0.4 could not reach the target
- `v0.4` used `32 samples/frame` and `640 us/frame`.
- That means each frame carried `32 * 16 = 512` payload bits.
- The required packet rate was `800000 / 512 = 1562.5 pps`.
- Measured TX MAC latency was about `1087 us min` and `1574 us avg`, already
  longer than the requested `640 us` frame period.
- So the bottleneck was not first a radio-quality problem; it was that the
  whole send path could not drain frames quickly enough.

Main optimization idea in v0.5
- Reduce packet rate while keeping the same payload target.
- Move from `32` to `96` samples per frame:
  - one frame now carries `96 * 16 = 1536` payload bits
  - target packet rate becomes `800000 / 1536 = 520.8 pps`
- Increase air-interface headroom at the same time:
  - `1 Mbps + ACK` -> `4 Mbps preferred + no-ACK`
- Reduce software-path overhead:
  - LP inactive software queue removed from the hot path
  - LP sends generated frames directly into IPC
  - HP TX queue depth and ESB payload/FIFO sizes increased

Code-level v0.5 direction
- Protocol and rate parameters:
  - `applications/rf_link_tx/src/proto.h`
  - `applications/rf_link_rx/src/proto.h`
  - frame payload changed to `96` samples
  - TX period changed to `1920 us`
- RF mode changes:
  - `applications/rf_link_tx/src/radio_link.c`
  - `applications/rf_link_rx/src/radio_link.c`
  - ESB changed to `4 Mbps preferred`
  - payload path changed to `no-ACK`
- Queue and runtime sizing:
  - `applications/rf_link_tx/src/tx_queue.c`
  - `applications/rf_link_tx/prj.conf`
  - `applications/rf_link_rx/prj.conf`
  - HP queue depth, ESB max payload, and FIFO sizes increased
- LP hot-path simplification:
  - `applications/rf_link_tx/flpr_app/src/main.c`
  - `applications/rf_link_tx/flpr_app/CMakeLists.txt`
  - direct frame generation to IPC send path

Why TX once looked dead while RX stayed zero
- After the v0.5 parameter change, TX initially showed no useful RF progress
  and RX stayed at `frames=0 samples=0 bps=0`.
- That symptom looked like a radio failure, but the real issue was inside the
  IPC path.

Zero-throughput debug logic
- First question: did LP still generate frames
  - solved by adding LP shared trace counters
- Second question: did HP receive IPC frames
  - solved by adding `ipc_bad_size`, `ipc_bad_magic`, and `ipc_rx`
- Third question: did frames reach the air
  - solved by keeping TX `rf_ok/rf_fail/rf_timeout/rf_err`
- Fourth question: did RX see air traffic but reject frames
  - solved by adding `rf_evt`, `rf_frames`, and `rf_read_err` on RX
- Fatal-path visibility:
  - `applications/rf_link_tx/src/fatal_diag.c`
  - `applications/rf_link_tx/flpr_app/src/fatal_diag.c`
  - HP could now print `HP fatal reason=...`
  - LP could now store fatal counters in shared trace memory

Actual root cause 1: ICMSG send success was interpreted incorrectly
- File:
  - `applications/rf_link_tx/flpr_app/src/ipc_tx.c`
- Problem:
  - LP treated only return `0` from `ipc_service_send()` as success
- Reality:
  - Zephyr ICMSG may return the sent byte count on success
- Fix:
  - accept both `0` and `sizeof(frame)` as success
  - retry `-ENOMEM`, `-EAGAIN`, `-EBUSY`, and `-ENOBUFS`

Actual root cause 2: ICMSG default PBUF RX buffer was too small
- New wire frame size:
  - `204 bytes`
- Zephyr default:
  - `CONFIG_PBUF_RX_READ_BUF_SIZE=128`
- Result:
  - once real 204-byte traffic started, ICMSG asserted internally and TX hit
    `HP fatal reason=4`
- Fix:
  - `applications/rf_link_tx/prj.conf`
  - `applications/rf_link_tx/flpr_app/prj.conf`
  - set `CONFIG_PBUF_RX_READ_BUF_SIZE=256`
  - `applications/rf_link_tx/src/proto.h`
  - add compile-time assert so future frame growth cannot silently exceed PBUF

Stable intermediate state after IPC/PBUF fixes
- Example stable excerpt:
  - `TX stat sent=51857 ipc_rx=51857 queued=51857 q_drop=0 ...`
  - `RX stat ... bps=774144 lost=0 ...`
- Meaning:
  - LP generation, IPC receive, HP queueing, and RF send were all in lockstep
  - the remaining issue was not corruption or packet loss in the active path

Why there was still a 3 percent gap
- With `96 samples/frame` and `1920 us` target period:
  - target packet rate is `520.8 pps`
- But the stable excerpt gave:
  - `774144 / 1536 = 504 pps`
- Reason:
  - LP used relative `k_sleep(K_USEC(1920))`
  - real frame period became `processing time + 1920 us`

Final timing fix
- File:
  - `applications/rf_link_tx/flpr_app/src/main.c`
- Change:
  - use `k_cycle_get_64()` and absolute deadline pacing
  - every loop targets the next fixed timestamp instead of sleeping a constant
    delay after work completes
- Control switch:
  - `applications/rf_link_tx/src/proto.h`
  - `RF_LINK_LP_USE_ABSOLUTE_PACING`
  - set to `0` to return to the old relative pacing without reverting code

Current understanding
- The final near-800 kbps state was not achieved by only changing PHY and ACK.
- It required all of the following together:
  - larger frames
  - lower packet rate
  - no-ACK high-rate RF mode
  - direct LP->IPC hot path
  - corrected ICMSG send semantics
  - correct PBUF sizing for 204-byte frames
  - absolute-deadline pacing on LP

Version focus: v0.6 RX sample stream export
-------------------------------------------

Goal
- Export real accepted RX frames to the host PC without rewriting the RF link.
- Keep the default `RX stat ...` workflow unchanged unless export mode is enabled.
- Prepare a stable handoff path for future real ADC data and downstream processing.

Why the old UART statistics path was not enough
- `save_serial_csv.py` records only `RX stat ...` and `TX stat ...`.
- The RX firmware printed counters and sample boundaries, not the 96-sample payload array.
- Even after replacing the fake TX sampler with real ADC, the old statistics UART would still not expose full sample data.

Design choice for v0.6
- Do not stream raw samples from the RF callback directly.
- Keep RX validation and sequence/loss accounting in place.
- After a frame is accepted, copy it into a small buffered export queue.
- Drain that queue from a dedicated thread that writes fixed-size binary records on uart30.
- Keep this path behind `CONFIG_RF_LINK_RX_SAMPLE_STREAM` so the default build remains the current stable one.

Why not keep using text UART output
- `16-bit * 50 ksps` payload is about `800 kbps` before any UART framing overhead.
- Text CSV on UART would consume much more bandwidth than the RF payload itself.
- A binary fixed-size record is much closer to the real payload size and is easier to parse reliably.

Code-level v0.6 direction
- RX firmware:
  - `applications/rf_link_rx/src/rx_sample_stream.c`
  - `applications/rf_link_rx/src/rx_sample_stream.h`
  - `applications/rf_link_rx/src/rx_reorder.c`
  - `applications/rf_link_rx/src/main.c`
- Build/config:
  - `applications/rf_link_rx/stream.conf`
  - `applications/rf_link_rx/sample.yaml`
- Host capture:
  - `dump_rx_frames.py`

Binary record format
- Each UART export record is fixed at `224 bytes`.
- Record layout:
  - stream magic
  - record length
  - stream version
  - stream frame index
  - cumulative stream drop count
  - RX uptime in ms
  - one complete `rf_frame`
- The PC script resynchronizes on the stream magic and writes one CSV row per RF frame.

How v0.6 keeps the current link stable
- Default `build_rf_link_rx` still prints `RX stat ...` at 115200 baud.
- Stream export is enabled only through `stream.conf`.
- The RF path, protocol, queueing, and TX side are unchanged.
- The only hot-path addition is one bounded copy into a non-blocking queue after frame acceptance.

Current limitation
- v0.6 is validated locally at build and parser-format level.
- Hardware validation of continuous stream capture is still needed.
- The first hardware check should confirm:
  - RF throughput remains near the current working state
  - `stream_drop_total` remains low during capture
  - the PC can sustain long CSV writes at the chosen baud

Version focus: IIM-42352 4096-sample batch integration (untagged)
----------------------------------------------------------------

Goal
- Replace the FLPR fake ramp with real IIM-42352 acquisition.
- Let CPUAPP block in System ON idle until FLPR has 4096 int16 samples.
- Preserve the main repository's 204-byte ESB frame and RX implementation.

Reference repository audit
- Compared main baseline `f635103` with `Ins1ghtt/2.4g-mems-` tip `371be76`.
- The repositories had no usable common Git history, so changes were audited
  file by file instead of merged as histories.
- Accepted the IIM-42352 wiring and FIFO acquisition intent.
- Rejected per-frame HP binary UART output because it competes with the real
  ESB output path.
- Rejected unrelated RX DPPI, board Kconfig, document attachment, and archive
  changes.

Reference implementation issues corrected
- The reference code used a 1024-byte FIFO watermark but read at most 256 bytes.
- FIFO count was read high-byte first even though FIFO_COUNTL latches both bytes.
- The default 100 us interrupt pulse was left enabled at 4 kHz ODR; the
  datasheet requires the 8 us selection at ODR >= 4 kHz.
- Packet validation checked only header bit 6 rather than Packet 1's full
  `0100_00xx` pattern.
- The reference ring contained only 384 int16 values and still sent 96-sample
  IPC frames, so it did not implement the requested 4096-sample handoff.

Final implementation
- Added two cache-line-aligned shared SRAM slots, each holding 4096 int16 samples.
- Added CRC32-protected ICMsg batch-ready and batch-release descriptors.
- FLPR owns a slot while filling; CPUAPP owns it after ready; FLPR cannot reuse
  it until the matching sequence release arrives.
- CPUAPP waits forever on a batch queue, so the kernel can idle the core and a
  VEVIF mailbox interrupt wakes it.
- CPUAPP initializes ESB lazily on the first valid batch.
- One batch becomes 42 full 96-sample frames and one 64-sample final frame.
- Added MEMS, batch-start, and batch-end RF flags without changing the 204-byte
  wire size.
- Added `verify_mems_batches.py` for captured CSV validation.

Offline validation
- NCS 3.1.0 TX sysbuild passed for CPUAPP and FLPR.
- FLPR RAM: about 58.3 KB of 96 KB.
- TX CPUAPP RAM: about 25.1 KB of 160 KB.
- RX CPUAPP build passed; RAM: about 19.2 KB of 188 KB.
- No hardware was attached. SPI electrical behavior, INT1 capture, RF loss,
  power, and long-run stability remain explicit bench-test items.

Version focus: nRF V2 software preintegration for four-channel/ZYNQ reception
-------------------------------------------------------------------------------

Goal
- Preserve the proven TX dual-core acquisition path and 204-byte V1 ESB frame.
- Prepare RX firmware for four independent links, a common ZYNQ SYNC pulse,
  explicit logical sample indices, and a future command-oriented SPIS path.
- Keep default RX and UART stream builds usable without ZYNQ or SYNC wiring.

Repository and callback audit
- Baseline branch: codex/rf-link-2p4g-devlog at d40f02e.
- Worktree was clean before the change.
- NCS 3.1.0 / Zephyr 4.1.99 build provenance was retained.
- Removed the ineffective TX CPUAPP `CONFIG_PRINTK=n` assignment because the
  enabled NCS boot banner already selects PRINTK; generated behavior is
  unchanged and the Kconfig conflict warning is gone.
- The ESB event handler is called from the direct ESB event IRQ.
- The old handler performed validation and sequence accounting in that IRQ.
- RX UART30 occupies the spi30/uart30 shared peripheral address and P0.0/P0.1.
- Available but disabled CPUAPP SPI targets include spi00, spi20, spi21,
  spi22, and spi30; TIMER00/10/20-24, GPIOTE20/30, and DPPI domains are visible.

Concurrency change
- The ESB callback now only reads payloads, takes a timebase tick, and uses a
  K_NO_WAIT put into a statically allocated radio queue.
- All validation, CRC, sequence extension, synchronization, and transport work
  moved to the RX main thread.
- Default RX uses 16 deferred radio records; the V2 preview uses 64.
- Queue full always increments a counter and drops the new record. No old data
  is silently overwritten.

V2 software-preintegration modules
- rf_link_future.h: internal metadata and fixed 248-byte FPGA record.
- frame_queue: 64-record static queue with peek and explicit commit.
- rx_reorder: 16-to-32-bit wrap, forward loss, duplicate/late classification,
  missing intervals, logical index, and INDEX_ESTIMATED.
- timebase: one API for receive ticks and software SYNC capture injection.
- sync_manager: IDLE/ARMED/WAIT_START/ALIGNING/LOCKED/DEGRADED/ERROR.
- fpga_transport: selected-backend ownership and record construction.
- fpga_spi_transport: GET/STATUS/PEEK/READ/COMMIT/DROP/control protocol engine.
- link_control: bounded ARM/START/STOP/RESET command state.

Wire compatibility
- The air frame remains rf_frame V1 and exactly 204 bytes.
- protocol_version, node_id, extended sequence, sync_epoch, rx_tick, and
  logical_sample_index are receiver-side metadata only.
- Missing V1 frame sample counts are estimated at the maximum 96 samples and
  marked as estimates; samples are never fabricated by repeating old data.

Connect Kit Rev.A pin audit
- P0.2 is LED0, P0.3 is USER, and P0.0/P0.1 are UART30, so they are excluded
  from the preferred V2 connector group.
- Candidate review mapping: P1.4 SCK, P1.5 MOSI, P1.6 MISO, P1.7 CSN,
  P1.9 DRDY, and P1.10 SYNC_IN; candidate peripheral spi21.
- This mapping is documented but not enabled in the default overlay. The ZYNQ
  adapter schematic must confirm it before physical SPIS/SYNC code is enabled.

Offline validation so far
- Default RX: FLASH 62396 B, RAM 22776 B, passed.
- V2-preview RX: FLASH 68860 B, RAM 48960 B, passed.
- V2-preview RX with all transports disabled: FLASH 65852 B, RAM 48624 B,
  passed.
- The builds retain known NCS/Zephyr configuration/devicetree warnings; no
  application compile or link error was present.
- Thirteen host contract tests passed, covering the twelve requested behavior
  groups plus source structure constants.
- No hardware was attached or flashed during this implementation; the listed
  results are offline build and host-test evidence only.

Release documentation and verification: V1 baseline plus V2-V6 roadmap
------------------------------------------------------------------------

- Version terminology was frozen: V1 is the two-board single-link baseline;
  `future.conf` is a V2 software-preintegration profile, not a completed V2.
- Added `rf_link_ROADMAP.md` with the V1 two-board test matrix, measurements,
  acceptance gates, and staged V2-V6 dependencies.
- V2 is single-link ZYNQ SPIS/DRDY/SYNC integration; V3 is four-link logical
  aggregation; V4 introduces the second-generation air protocol; V5 targets
  physical sample synchronization and drift control; V6 is system hardening.
- Rebuilt TX sysbuild, default RX, stream RX, V2-preview RX, and V2-preview RX
  without transport under NCS 3.1.0. All builds completed successfully.
- Re-ran thirteen host contract tests and `git diff --check`; both passed.
- Hardware was still unavailable, so the V1 board tests remain an explicit
  user-executed acceptance gate rather than a claimed result.
